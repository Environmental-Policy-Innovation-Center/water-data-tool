# FRONTEND REFACTOR

This `/frontend_refactor` directory is a temporary directory which is holding notes and resources which are helpful in our goal of getting the FE architecture refactored into a clean and conventional manner using Hotwire + Tailwinds CSS + ViewComponent.

Once all of this work is completed, a single clean doc will be added to the `/docs` dir which captures the end state architecture of the Frontend, along with decisions.